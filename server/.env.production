# Production Environment Variables
# Copy these to your deployment platform's environment variables section

PORT=4002
NODE_ENV=production

# MongoDB Atlas (already configured)
MONGO_URI=mongodb+srv://bhimachetan1:8lJr9idGvlJRodRe@cluster0.adpptxr.mongodb.net/chat_app?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret (CHANGE THIS FOR PRODUCTION!)
JWT_SECRET=your_super_secure_jwt_secret_key_change_me_in_production

# CORS Origins (update with your deployed frontend URL)
CLIENT_ORIGIN=https://your-frontend-domain.com,https://your-expo-app.expo.dev
